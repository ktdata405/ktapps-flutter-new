import 'package:flutter/material.dart';

class MilkReportScreen extends StatelessWidget {
  const MilkReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Milk Report')),
      body: const Center(child: Text('Milk report is not yet migrated')),
    );
  }
}

